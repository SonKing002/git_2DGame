using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Main;

namespace Main
{
    //사용방법 참고.https://etst.tistory.com/31
    public class TestRead : MonoBehaviour
    {
        void Start()
        {
            /*
            List<Dictionary<string, object>> data = CSVReader.Read("Player_Weapon_ColiderList");
            //data가 2차원 배열의 형태로 생성됨

            for (var i = 0; i < data.Count; i++)
            {
                Debug.Log( "Weapon : " + data[i]["Weapon"]
                         + "colider Offset x:" + data[i]["OffsetX"]
                         + "colider Offest y:" + data[i]["OffsetY"]
                         + "colider Size x:" + data[i]["SizeX"]
                         + "colider Size y:" + data[i]["SizeY"] );
            }
            */

        }

    }

}